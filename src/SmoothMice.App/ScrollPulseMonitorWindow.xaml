<Window x:Class="SmoothMice.App.ScrollPulseMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SmoothMice.App.ViewModels"
        Title="Monitorar scroll · SmoothMice"
        Width="680"
        Height="520"
        MinWidth="560"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.Surface}"
        FontFamily="Segoe UI Variable, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Loaded="Monitor_OnLoaded"
        Closed="Monitor_OnClosed">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="MonitorLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Muted}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="5,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="ToolTip" Value="{Binding Markers}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAnomalous}" Value="True">
                    <Setter Property="Background" Value="#FFF7ED"/>
                    <Setter Property="Foreground" Value="#9A3412"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="14,12,14,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <Button Content="Limpar"
                    DockPanel.Dock="Right"
                    Style="{StaticResource SecondaryButton}"
                    Padding="10,5"
                    Click="Clear_OnClick"
                    ToolTip="Zera os pulsos desta visualização para iniciar um teste controlado"
                    AutomationProperties.Name="Limpar monitor de scroll"/>
            <StackPanel>
                <TextBlock Text="Monitor de scroll" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}"/>
                <TextBlock Text="Pulsos físicos antes da suavização" Margin="0,2,0,0" FontSize="11" Foreground="{StaticResource Brush.Muted}"/>
            </StackPanel>
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Text="{Binding StatusText}"
                   Margin="0,10,0,8"
                   FontSize="11"
                   Foreground="{StaticResource Brush.Muted}"
                   TextWrapping="Wrap"
                   AutomationProperties.LiveSetting="Polite"/>

        <Grid Grid.Row="2" Margin="0,0,0,9">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding TotalText}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}"/>
                <TextBlock Text="capturados" Style="{StaticResource MonitorLabel}"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding AxisText}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}"/>
                <TextBlock Text="por eixo" Style="{StaticResource MonitorLabel}"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="{Binding BurstText}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}"/>
                <TextBlock Text="pulsos em rajada (120 ms)" Style="{StaticResource MonitorLabel}"/>
            </StackPanel>
            <StackPanel Grid.Column="3">
                <TextBlock Text="{Binding ReversalText}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}"/>
                <TextBlock Text="até 150 ms" Style="{StaticResource MonitorLabel}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3">
            <ListView x:Name="PulseList"
                      ItemsSource="{Binding Rows}"
                      BorderBrush="{StaticResource Brush.Border}"
                      BorderThickness="1"
                      Background="{StaticResource Brush.Card}"
                      ScrollViewer.CanContentScroll="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      AutomationProperties.Name="Pulsos físicos de scroll">
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Header="TEMPO" Width="82" DisplayMemberBinding="{Binding RelativeTime}"/>
                        <GridViewColumn Header="UTC" Width="102" DisplayMemberBinding="{Binding UtcTime}"/>
                        <GridViewColumn Header="EIXO" Width="78" DisplayMemberBinding="{Binding Axis}"/>
                        <GridViewColumn Header="PULSO" Width="72" DisplayMemberBinding="{Binding DeltaAndDirection}"/>
                        <GridViewColumn Header="INTERVALO" Width="75" DisplayMemberBinding="{Binding Interval}"/>
                        <GridViewColumn Header="JANELA" Width="82" DisplayMemberBinding="{Binding BurstWindow}"/>
                        <GridViewColumn Header="MARCADORES" Width="125" DisplayMemberBinding="{Binding Markers}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <TextBlock Text="Gire a roda para começar"
                       Visibility="{Binding EmptyStateVisible, Converter={StaticResource BoolToVis}}"
                       IsHitTestVisible="False"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="{StaticResource Brush.Muted}"/>
        </Grid>
    </Grid>
</Window>
